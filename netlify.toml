[build]
  command   = "sed -i \"s/__HASH__/${COMMIT_REF:0:7}/\" index.html"
  publish   = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[context.production.environment]
  NODE_VERSION = "18"
